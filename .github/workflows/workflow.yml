name: Publish Python Package

on:
  release:
    types: [created]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'  # Substitua pela versão do Python que você utiliza

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build

    - name: Build the package
      run: python -m build

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__  # Indica que será utilizado um token de API
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}  # Passa o token do PyPI que você configurou como secret
      run: |
        pip install twine
        twine upload dist/*
